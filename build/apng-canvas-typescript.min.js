var parse = function (buffer) {
};

var APNG = /** @class */ (function () {
    function APNG() {
    }
    /**
     * @param buffer
     * @return {Promise}
     */
    APNG.prototype.parseBuffer = function (buffer) {
        return parse();
    };
    APNG.prototype.parseURL = function () {
    };
    APNG.prototype.animateImage = function () {
    };
    return APNG;
}());

export default APNG;
